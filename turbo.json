{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["build/**", "dist/**"],
      "env": ["REACT_APP_BASEPATH"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["REACT_APP_BASEPATH"]
    },
    "db:push": {
      "cache": false
    },
    "prepare": {
      "cache": false
    },
    "predev": {
      "cache": false,
      "dependsOn": ["prepare"]
    }
  }
}
