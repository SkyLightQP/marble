{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["build/**", "dist/**"],
      "env": ["REACT_APP_BASEPATH"]
    },
    "prepare": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["REACT_APP_BASEPATH"]
    },
    "db:push": {
      "cache": false
    }
  }
}
